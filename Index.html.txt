<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Lunet</title>

<style>
* {
    box-sizing: border-box;
}

html, body {
    margin: 0;
    width: 100%;
    height: 100%;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    background: #212121;
    color: white;
}

/* APP */

.app {
    width: 100%;
    height: 100vh;
    display: flex;
}

/* SIDEBAR */

.sidebar {
    width: 250px;
    height: 100%;
    background: #171717;
    padding: 16px;
    display: flex;
    flex-direction: column;
}

.logo {
    font-size: 24px;
    font-weight: bold;
    padding: 10px;
    margin-bottom: 20px;
}

.new-chat {
    width: 100%;
    padding: 12px;
    border: 1px solid #444;
    border-radius: 10px;
    background: #242424;
    color: white;
    cursor: pointer;
    font-size: 15px;
}

.new-chat:hover {
    background: #303030;
}

.sidebar-bottom {
    margin-top: auto;
    padding: 10px;
    color: #aaa;
}

/* MAIN */

.main {
    flex: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    position: relative;
}

/* TOP BAR */

.topbar {
    height: 60px;
    border-bottom: 1px solid #333;
    display: flex;
    align-items: center;
    padding: 0 22px;
    font-size: 18px;
    font-weight: bold;
}

/* CHAT */

.chat {
    flex: 1;
    overflow-y: auto;
    padding: 30px 20px 150px;
}

.welcome {
    text-align: center;
    margin-top: 18vh;
}

.welcome .moon {
    font-size: 55px;
    margin-bottom: 15px;
}

.welcome h1 {
    font-size: 32px;
    margin: 0 0 10px;
}

.welcome p {
    color: #999;
    margin: 0;
}

/* MESSAGES */

.messages {
    max-width: 800px;
    margin: auto;
}

.message {
    display: flex;
    gap: 12px;
    margin: 25px 0;
}

.avatar {
    width: 34px;
    height: 34px;
    min-width: 34px;
    border-radius: 50%;
    background: #303030;
    display: flex;
    align-items: center;
    justify-content: center;
}

.text {
    padding-top: 5px;
    line-height: 1.6;
    font-size: 15px;
    white-space: pre-wrap;
}

/* LOADING */

.loading {
    display: flex;
    align-items: center;
    gap: 5px;
    height: 30px;
}

.loading span {
    width: 7px;
    height: 7px;
    background: #aaa;
    border-radius: 50%;
    animation: loading 1.2s infinite;
}

.loading span:nth-child(2) {
    animation-delay: 0.15s;
}

.loading span:nth-child(3) {
    animation-delay: 0.3s;
}

@keyframes loading {
    0%, 60%, 100% {
        opacity: 0.3;
        transform: translateY(0);
    }

    30% {
        opacity: 1;
        transform: translateY(-5px);
    }
}

/* INPUT */

.input-area {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 20px;
    background: linear-gradient(
        transparent,
        #212121 25%
    );
}

.input-box {
    max-width: 800px;
    margin: auto;
    min-height: 55px;
    background: #2f2f2f;
    border: 1px solid #444;
    border-radius: 16px;
    display: flex;
    align-items: flex-end;
    padding: 8px;
}

textarea {
    flex: 1;
    resize: none;
    border: 0;
    outline: 0;
    background: transparent;
    color: white;
    font-family: inherit;
    font-size: 15px;
    padding: 10px;
    max-height: 150px;
}

textarea::placeholder {
    color: #999;
}

.send {
    width: 38px;
    height: 38px;
    border: 0;
    border-radius: 50%;
    background: white;
    color: black;
    font-size: 20px;
    cursor: pointer;
}

.send:hover {
    transform: scale(1.05);
}

/* MOBILE */

@media (max-width: 700px) {

    .sidebar {
        display: none;
    }

    .welcome h1 {
        font-size: 27px;
    }

    .input-area {
        padding: 12px;
    }
}
</style>
</head>

<body>

<div class="app">

    <!-- SIDEBAR -->

    <aside class="sidebar">

        <div class="logo">
            🌙 Lunet
        </div>

        <button class="new-chat" onclick="newChat()">
            ＋ New chat
        </button>

        <div class="sidebar-bottom">
            ✨ Lunet AI
        </div>

    </aside>


    <!-- MAIN -->

    <main class="main">

        <div class="topbar">
            Lunet
        </div>

        <div class="chat" id="chat">

            <div class="welcome" id="welcome">

                <div class="moon">
                    🌙✨
                </div>

                <h1>How can I help?</h1>

                <p>Ask Lunet anything.</p>

            </div>

            <div class="messages" id="messages"></div>

        </div>


        <!-- INPUT -->

        <div class="input-area">

            <div class="input-box">

                <textarea
                    id="input"
                    rows="1"
                    placeholder="Message Lunet..."
                ></textarea>

                <button class="send" onclick="sendMessage()">
                    ↑
                </button>

            </div>

        </div>

    </main>

</div>


<script>

const input = document.getElementById("input");
const messages = document.getElementById("messages");
const welcome = document.getElementById("welcome");
const chat = document.getElementById("chat");


/* ENTER TO SEND */

input.addEventListener("keydown", function(event) {

    if (event.key === "Enter" && !event.shiftKey) {

        event.preventDefault();

        sendMessage();

    }

});


/* AUTO RESIZE */

input.addEventListener("input", function() {

    this.style.height = "auto";

    this.style.height =
        Math.min(this.scrollHeight, 150) + "px";

});


/* SEND */

function sendMessage() {

    const text = input.value.trim();

    if (text === "") return;

    welcome.style.display = "none";

    addMessage(text, true);

    input.value = "";

    input.style.height = "auto";


    /* LOADING */

    const loading = document.createElement("div");

    loading.className = "message";

    loading.id = "loading";

    loading.innerHTML = `
        <div class="avatar">🌙</div>

        <div class="loading">
            <span></span>
            <span></span>
            <span></span>
        </div>
    `;

    messages.appendChild(loading);

    scrollBottom();


    /* TEMPORARY AI RESPONSE */

    setTimeout(function() {

        loading.remove();

        const response = getResponse(text);

        addMessage(response, false);

    }, 1000);

}


/* ADD MESSAGE */

function addMessage(text, user) {

    const message = document.createElement("div");

    message.className = "message";

    message.innerHTML = `
        <div class="avatar">
            ${user ? "🙂" : "🌙"}
        </div>

        <div class="text"></div>
    `;

    message.querySelector(".text").textContent = text;

    messages.appendChild(message);

    scrollBottom();

}


/* BASIC LUNET BRAIN */

function getResponse(text) {

    const t = text.toLowerCase();


    if (
        t.includes("hello") ||
        t.includes("hi") ||
        t.includes("hey")
    ) {
        return "Hey! 👋 I'm Lunet. What are we doing today?";
    }


    if (
        t.includes("who are you") ||
        t.includes("what are you")
    ) {
        return "I'm Lunet 🌙✨ — your AI assistant.";
    }


    if (t.includes("how are you")) {
        return "I'm doing great! 🤖✨";
    }


    if (t.includes("what is your name")) {
        return "My name is Lunet. 🌙";
    }


    if (t.includes("thank")) {
        return "You're welcome! 😊";
    }


    if (t.includes("bye")) {
        return "See you later! 🌙";
    }


    return "I'm still learning! 🧠✨ We can make my brain much smarter next.";
}


/* NEW CHAT */

function newChat() {

    messages.innerHTML = "";

    welcome.style.display = "block";

    input.focus();

}


/* SCROLL */

function scrollBottom() {

    setTimeout(function() {

        chat.scrollTop = chat.scrollHeight;

    }, 50);

}

</script>

</body>
</html>
